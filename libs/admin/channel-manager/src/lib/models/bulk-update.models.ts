import { BulkUpdateForm, BulkUpdateRequest } from '../types/bulk-update.types';

export class FormFactory {
  static makeRatesRequestData(formData: BulkUpdateForm) {
    let modifiedData: BulkUpdateRequest[] = [];
    return modifiedData;
  }
}
