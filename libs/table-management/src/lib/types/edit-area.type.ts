export type AreaForm = {
  id: string;
  name: string;
  shortDescription: string;
};

export type AreaFormDataResponse = {
  created: number;
  description: string;
  id: string;
  name: string;
  status: true;
  updated: number;
  shortDescription: string;
};
